name: Generate Dot Matrix Portrait

on:
  push:
    branches:
      - main
    paths:
      - "assets/portrait-source.png"
      - "scripts/dotify.py"
      - ".github/workflows/dotmatrix.yml"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install Pillow

      - name: Generate Matrix dot portrait
        run: |
          python scripts/dotify.py \
            assets/portrait-source.png \
            --out assets/portrait.svg \
            --cols 160 \
            --cell 5.5 \
            --dot-scale 0.92 \
            --contrast 1.55 \
            --gamma 0.82 \
            --detail 0.85 \
            --floor 0.035 \
            --fall-time 1.15 \
            --max-delay 3.2 \
            --seed 42

      - name: Commit generated portrait
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add assets/portrait.svg

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Generate dot matrix portrait"
          git push
